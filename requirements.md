# 文件整理助手 organize.py — 需求文档

> 状态：**已确认**（v1 冻结）
> 技术栈：Python 3，仅用标准库（argparse / pathlib / shutil / json）

## 一句话描述

一个命令行工具：指定一个文件夹，按文件类型自动归类到子文件夹；先预览、后执行、可撤销。

## 功能清单（v1）

1. **分类**：按扩展名将文件归入以下类别文件夹：
   - `Images`（图片）：jpg, jpeg, png, gif, webp, bmp, svg, ico, heic, raw
   - `Documents`（文档）：pdf, doc, docx, xls, xlsx, ppt, pptx, txt, md, csv, json, xml, epub
   - `Videos`（视频）：mp4, mov, avi, mkv, wmv, flv, webm, m4v
   - `Audio`（音频）：mp3, wav, flac, aac, ogg, m4a, wma
   - `Archives`（压缩包）：zip, rar, 7z, tar, gz, bz2, xz
   - `Code`（代码）：py, js, ts, java, c, cpp, h, go, rs, html, css, sh, sql
   - `Others`（其他）：未匹配到以上任何类别的文件

2. **两种运行模式**：
   - **预览模式（默认）**：只打印「将把 文件A → Images/文件A」这类计划，**不移动任何文件**
   - `--apply`：真正执行移动

3. **重名安全**：目标位置已有同名文件时，自动加序号（`photo.png` → `photo_1.png` → `photo_2.png`），**绝不覆盖已有文件**。

4. **撤销**：`--undo` 撤销**最近一次** `--apply` 整理（把文件移回原位置）。每次执行移动时，把操作记录写入日志文件。

## 命令行接口

```text
python organize.py <目录>            # 预览模式（默认）
python organize.py <目录> --apply    # 执行整理
python organize.py --undo            # 撤销最近一次整理
python organize.py --help            # 帮助
```

## 边界与规则

- **不动子文件夹**：只整理目标目录下的直接文件，不递归进入子文件夹，也不移动任何文件夹本身。
- **不动隐藏文件**：跳过以 `.` 开头的文件。
- **不动自己**：跳过 organize.py 本身及其日志文件。
- **幂等**：对已整理过的目录再运行，应显示「无需整理」或几乎没有可移动项。
- **空类别文件夹**：不预先创建所有类别文件夹，只在有文件需要移入时才创建；撤销时若原目录因移动变空，可一并清理（仅限本次整理创建的目录）。

## 范围控制（v1 明确不做）

- ❌ 图形界面
- ❌ 文件夹监听 / 自动整理
- ❌ 自定义分类规则（v2 考虑 JSON 配置文件）
- ❌ 跨设备 / 云同步

## 成功标准

1. 对满是散文件的测试目录运行预览 → 输出清晰、可读、无误导
2. `--apply` 后文件确实归类，且无文件丢失、无覆盖
3. `--undo` 后目录完整还原（文件回到原位置，名字不变）
4. 对已整理目录再运行 → 不做多余移动
5. 运行期间任何报错都不应导致文件损坏

## 测试计划

- 手工测试目录：创建含各种类型、重名、隐藏文件、子文件夹、空目录的测试文件夹
- 测试场景：
  a) 预览模式不产生任何移动
  b) apply 后文件归类正确
  c) 重名文件加序号
  d) undo 完整还原
  e) 重复运行幂等
  f) 不存在的目录 → 友好报错退出
